class Config:
    APP_BASE_URL = "https://barrigarest.wcaquino.me"
    APP_PORT = "443"  # Porta padrão para HTTPS; use "80" para HTTP
    APP_BASE_PATH = "/"
    APP_CONTENT_TYPE = "application/json"
    MAX_TIMEOUT = 1  # Tempo limite em segundos
    ENV = "dev"  # Pode ser 'dev', 'staging' ou 'prod'
